import 'package:flutter/material.dart';

class CreateMessageWidget extends StatelessWidget {
  const CreateMessageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
